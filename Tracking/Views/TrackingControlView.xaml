<UserControl x:Class="Tracking.Views.TrackingControlView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Tracking.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:TrackingControlViewModel}">
    <StackPanel Orientation="Horizontal">
        <Button Content="LoadTrack" Command="{Binding LoadTrack}" />
        <Button Content="SaveTrack" Command="{Binding SaveTrack}" />
    </StackPanel>
</UserControl>