<UserControl x:Class="SimpleMsul.Views.EmulatorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SimpleMsul.Views"
             xmlns:views="clr-namespace:MapViewer.Emulation.Msul.Views;assembly=MapViewer.Emulation.Msul"
             xmlns:viewModels="clr-namespace:SimpleMsul.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:EmulatorViewModel}"
             Width="400" Height="600">
    <Grid>
        <views:MsulEmulationParametersView DataContext="{Binding Parameters}" />
    </Grid>
</UserControl>