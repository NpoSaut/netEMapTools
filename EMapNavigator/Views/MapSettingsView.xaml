<UserControl x:Class="EMapNavigator.Views.MapSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:EMapNavigator.ViewModels"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance viewModels:MapSettingsViewModel}">
    <Grid>
        <CheckBox IsChecked="{Binding UseHighResolutionTiles}">
            <StackPanel>
                <TextBlock Text="Карта высокого разрешения" />
                <TextBlock Text="Потребуется перезагрузка приложения" FontSize="9" Opacity="0.7" />
            </StackPanel>
        </CheckBox>
    </Grid>
</UserControl>