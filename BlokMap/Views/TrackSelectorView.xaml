<UserControl x:Class="BlokMap.Views.TrackSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:BlokMap.ViewModels"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300" d:DataContext="{d:DesignInstance viewModels:TrackSelectorViewModel}">
    <Grid>
        <ComboBox VerticalAlignment="Top" HorizontalAlignment="Right" Margin="15" IsReadOnly="True"
                  ItemsSource="{Binding Tracks}" DisplayMemberPath="Name"
                  SelectedValue="{Binding SelectedTrack}" SelectedValuePath="Number" />
    </Grid>
</UserControl>